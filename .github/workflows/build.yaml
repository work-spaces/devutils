name: Build

on:
  workflow_dispatch:
    inputs:
      target:
        description: "spaces target to build (e.g. //devutils:install)"
        required: true
        type: string

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-14
          - runner: ubuntu-22.04
          - runner: ubuntu-22.04-arm

    runs-on: ${{ matrix.runner }}

    steps:
      - uses: work-spaces/spaces-checkout-run@73d3c2eb5f0d8671de2fccd4ac3cf6ec08ec95ad
        with:
          checkout: --url=https://github.com/work-spaces/devutils --rev=${{ github.ref_name }}
          run: ${{ inputs.target }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
